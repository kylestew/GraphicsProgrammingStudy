export function flow(canvas) {
    const options = {
        premultipliedAlpha: false,
        alpha: true,
    }

    const gl = canvas.getContext('webgl2', options)
    gl.clearColor(0, 0, 0, 0)

    // var maxParticleCount = ...

    let randomNumbers = [];

    https://github.com/dli/flow/blob/master/flow.js
}
